{
  "name": "modest-coder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "nuxt generate && gh-pages -d dist"
  },
  "dependencies": {
    "core-js": "^3.19.0",
    "nuxt": "^2.15.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
